#include <stdio.h>
#include <string.h>
int main(){
   int A,B,a=0,b=0;
   scanf("%d %d",&A,&B);
   while(A>0){
      a+=A%10;
      a*=10;
      A/=10;  
   }
   while(B>0){
      b+=B%10;
      b*=10;
      B/=10;  
   }
   a=a/10;
   b=b/10;
   if(a>b){
      printf("%d",a);
   }
   else{
      printf("%d",b);
   }

   return 0;
}
